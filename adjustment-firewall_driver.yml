- hosts: controller
  remote_user: heat-admin
  gather_facts: false
  vars:
    ansible_become: true
    driver: openvswitch
  roles:
  - neutron-firewall

- hosts: compute
  remote_user: heat-admin
  gather_facts: false
  vars:
    ansible_become: true
    driver: openvswitch
  roles:
  -  neutron-firewall


